- name: Install packages using loop
  hosts: localhost
  connection: local
  tasks:
    - name: Install packages
      ansible.builtin.package:
        name: "{{ item }}"
        state: latest
      loop:
        - nginx
        - mysql-server
        - zip