- name: Install packages using loop
  hosts: localhost
  connection: local
  become: true
  tasks:
    - name: Install packages
      ansible.builtin.package:
        name: "{{ item }}"  # item is the default variable name for the current item in the loop
        state: latest
      loop:
        - nginx
        - mysql-server
        - zip