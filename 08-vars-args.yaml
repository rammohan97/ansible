- name: Varibales from arguments
  hosts: node
  tasks:
    - name: Print the credentials from arguments
      ansible.builtin.debug:
        msg: "The username is {{ username }} and the password is {{ password }}"

#  ansible-playbook -i inventory.ini -e ansible_user=ec2-user -e ansible_password=DevOps321 -e username=Ram -e password=mohan 08-vars-args.yaml